import { css } from 'styled-components';

import { components } from '../_variables';

export const uiComponentsBox = css`
  margin: ${components.marginY} ${components.marginX};
  padding: ${components.paddingY} ${components.paddingX};
`;
